orbs:
 docker: circleci/docker@1.5.0
 
version: 2.1
executors:
  docker-publisher:
  docker:
  - image: circleci/node:latest
  auth:
    username: uzzal2k5
    password: shafiq2k5
 
jobs:
  publishLatestToHub: 
  executor: docker-publisher
 
  steps: 
    - checkout
    - setup_remote_docker
    - run: 
        name: Publish Docker Image to Docker Hub
        command: |
          echo shafiq2k5 | docker login -u uzzal2k5 — password-stdin
          docker build -t uzzal2k5/node_todo_app .
          docker push uzzal2k5/node_todo_app:latest


workflows:
  version: 2
  build-master:
    jobs:
      - publishLatestToHub
